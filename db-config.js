export const DB_CONFIG = Object.freeze({
    connectionLimit: 5,
    host: "localhost",
    user: "root",
    database: "history",
    password: ""
});
/* For production
export const DB_CONFIG = Object.freeze({
    connectionLimit: 5,
    host: "localhost",
    port: "3306",
    user: "petr",
    database: "history",
    password: "********"
});*/
